# 3주차

## 개선하기

### DOM(Document Object Model)

HTML 태그 하나하나를 JS로 컨트롤할 수 있도록 만든 객체 체계  
트리 구조로 이뤄짐  
객체들 간의 위계구성을 만들어 계층적 구조로 이뤄진다

우리는 이제 HTML이 아닌 JS로 모든 화면을 동작할 수 있다는 것을 알게 되었다.  
개발을 할 때 이 HTML의 태그마다 JS 파일을 만들고, 개개인이 그 파일만 작업하면 편하다  
그래서 JS 파일 하나하나 : **component** => 함수처럼 쓸 수 있겠다.. 재사용성이 좋네... 유지보수가 좋아지네!  
근데 좀 많아지면... 이제 너무 비효율적이게 되지 않나....  
"JS 코드의 양이 너무 많아지네.."=> JS에 HTML을 넣어보면 되지 않을까?? 번역기(컴파일러)를 만들면 생산성이 올라가지 않을까? 오... => JSX(JavaScript Syntax Extension)!! Babel 컴파일러 또한 등쟝

이로인해 **SPA**(Single Page Application)의 시대가 도래하게 되었다. 즉 웹페이지에서 더 확장되어 앱처럼 동작하게 된것!

이런 SPA 라이브러리들이 대거 탄생하게 되었는데, 그중 지금 React의 점유율이 압도적인 상황이다.

## React 활용하기

JS파일을 컴포넌트처럼 활용해서, 함수를 태그와 같이 사용할 수 있게 해줌  
그리고 기존 HTML 태그와 구분을 위해 컴포넌트는 함수명, 파일명을 대문자로 시작한다

## REACT

react는 라이브러리임을 인지하자

## JavaScript의 리스트

Array(배열)을 사용해서 구현한다. 대괄호[]를 사용해서 선언을 한다.  
push(), pop(), unshift(), shift()등과 같은 내장 메서드를 사용할 수 있다.  
그중에서 map() 메서드를 알아보도록 하자

### map()

Array 인스턴스의 map() 메서드는 호출한 배열의 모든 요소에 주어진 함수를 호출한 결과로 채운 새로운 배열을 생성한다.

```JS
const array1 = [1, 4, 9, 16];

// Pass a function to map
const map1 = array1.map((x) => x * 2);

console.log(map1);
// Expected output: Array [2, 8, 18, 32]
```

위와 같이 했을 때 실행을 해보면 모든 배열 요소에 각각 함수를 호출한 결과로 채운 배열을 생성하게 된다. 안의 화살표 함수에선 매개변수에 2배를 한것을 받아 요소로 만들게 한다.
